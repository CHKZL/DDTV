<Window  x:Class="DDTV_GUI.DDTV_Window.PlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        xmlns:local="clr-namespace:DDTV_GUI"
        mc:Ignorable="d"
        
        Title="PlayWindow" Width="800" Height="460"
        Icon="/Resources/Img/UI/DDTV.ico"
        
        WindowStyle="None" AllowsTransparency="True" Background="#fb7299" MouseDown="GlowWindow_MouseDown" >
    <Grid>
        <Grid>
            
        </Grid>
        <Grid x:Name="PlayControl" Margin="0,422,0,0" Background="#6600A1D6">
         
        </Grid>
        <Grid Height="38" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="38">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ToggleButton BorderThickness="0" IsChecked="True" Style="{StaticResource ToggleButtonFlip}" Click="ToggleButton_Click">
                    <hc:StatusSwitchElement.CheckedElement>
                        <Border Background="#00a1d6">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="显" Foreground="{DynamicResource TextIconBrush}"/>
                        </Border>
                    </hc:StatusSwitchElement.CheckedElement>
                    <Border Background="#fb7299">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="隐" Foreground="{DynamicResource TextIconBrush}"/>
                    </Border>
                </ToggleButton>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
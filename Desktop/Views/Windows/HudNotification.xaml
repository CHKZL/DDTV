<Window x:Class="Desktop.Views.Windows.HudNotification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1040" d:DesignHeight="368"
        Width="1040" Height="368"
        Title="HudNotification"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <!-- 通知队列：新胶囊从顶部插入，旧的顺势下移；窗口高度在代码里按配置的上限动态调整（上限+1个槽位用于溢出过渡） -->
    <StackPanel x:Name="Stack" Margin="0,8,0,0"/>
</Window>
